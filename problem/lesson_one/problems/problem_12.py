"""
(12) 回帰のコード例では、
 X = [1, 2, 3, 4, 5] から y = [2, 4, 6, 8, 10] を学習するモデルを作っています。
 もし、次に X=6 を入力したら、(    ) が予測されると考えられます。

ヒント:
- 規則としては x2 (2倍) のルールを学習するはず
"""

def solve():
    """
    X=6 のとき、予測値はいくつになるでしょう？
    """
    return 
